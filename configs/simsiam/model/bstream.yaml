defaults:
  - backbone: resnet50

main_worker: 'worker_simsiam_bstream'
load_path: ''
sync_bn: True
num_updates: 4
buff_siz: 16384
buffer_type: 'simple' # none, simple, minred
limit_num_seen_coeff: -1
pretrained:
